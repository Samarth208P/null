# Generated verifier bytes are pinned in deployment manifests. Preserve the
# generator's formatting and line endings instead of reformatting signed builds.
contracts/src/generated/*.sol -text -whitespace
